- name: loops concept
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing packages
    ansible.builtin.package:
      # name: "{{ item }}"
      # state: installed
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    # - nginx
    # - mysql
    # - zip
    - {name: nginx, state: absent}
    - {name: mysql, state: absent}
    - {name: nginx, state: absent}